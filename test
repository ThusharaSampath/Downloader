function unZipFiles() {
  var folderId = '17b5chDiWrtmj4uOLVo4_VdcnrK4lE7hr'
  var zipFolder = DriveApp.getFolderById(folderID);

  var files = zipFolder.getFiles();
  while(files.hasNext()){
    var file = files.next();
    var fileType = file.getMimeType();
    Logger.log(fileType)
  }

}
